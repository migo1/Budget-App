<%= form_with(model: category) do |form| %>
  <% if category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="mb-4">
    <%= form.label :name, style: "display: block mb-2" %>
    <%= form.text_field :name, id: 'name', class: "w-full border border-gray-600 rounded-lg" %>
  </div>
  
  <div>
    <%= form.label :your_attribute, class:"block mb-2 text-sm font-medium text-gray-900" do %>
      Upload file
    <% end %>
    
    <%= form.file_field :image, id: 'file_input', class: "block w-full text-sm text-gray-900 border border-gray-300 rounded-lg cursor-pointer  dark:text-gray-700 focus:outline-none  dark:border-gray-600 dark:placeholder-gray-400" %>
  </div>



  <div>
    <%= form.submit "Save ", class: "w-full bg-green-500 py-2 text-white text-center mt-8 border border-gray-100 rounded-lg" %>
  </div>
  
<% end %>
